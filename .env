# 开发环境使用 Vite 代理，不需要配置 API_BASE
# Vite 代理会自动将 /api 请求转发到 http://116.62.78.157:8101
VITE_API_BASE=

# 是否启用 Mock 模式：true = 使用本地 mockData / 本地函数；false = 调用真实后端
# 默认已切换为 false（真实后端）。如需临时使用本地 mock 数据，将其改为 true。
VITE_USE_MOCK=false
